services:
    api:
        image: ghcr.io/CtrlAltElite-Devs/staging-mcwd-api:latest
        ports:
            - "5001:5001"
        env_file:
            - .env
        environment:
            - NODE_ENV=development
        deploy:
            replicas: 2
            restart_policy:
                condition: on-failure
